import React from 'react'

const header = () => {
  return (
    <nav>
        Get Ready To complete Your Daily Tasks
    </nav>
  );
}

export default header;

/*function Head1(){
    return(
        <>
    <nav 
        style={{
        backgroundColor: "brown",
        padding: "12rem",
        }}
    > 
        Header 
    </nav>
    <div> 
        we can use many tags after enclosing in an empty tag
    </div>
    </>
    );
}

export default Head1;*/

// use rfce for this snippet 
/*import React from 'react';
import "./header.css";

function Header(){
  return(
    // we can add inine style also 
    <nav 
        style={{
        backgroundColor: "brown",
        padding: "12rem",
        }}
    > 
        Header 
    </nav>
  );
}

export default Header;*/

// use rafe for arrow function snippet

// to use this component, first we need to export
//export default Head1;

// another method to export and import is 
//export {Head1, Head2};